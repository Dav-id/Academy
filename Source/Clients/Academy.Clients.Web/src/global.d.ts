interface Window {
    appConfig: {
        auth: {
            CLIENT_ID: string;
            DISCOVERY_ENDPOINT: string;
            ISSUER: string;
        };
        api: {
            API_BASE_URL: string;
        };
    };
}